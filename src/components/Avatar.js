import { Container } from "@material-ui/core"
import * as React from "react"

const Avatar = () => {
  return (
    <Container className="avatar-box" alignItems="center">
      <img className="avatar" />
    </Container>
  )
}

export default Avatar
